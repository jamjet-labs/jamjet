# Runtime overhead

Measures the steady-state overhead JamJet adds vs running the same agent without durable execution or policy enforcement.

**JamJet-specific claim:** the cost of agent action control should be measured against direct LLM calls — not against other runtimes.

Methodology and reproduction instructions to follow as benchmarks are ported from `jamjet-labs/jamjet-benchmarks`.
